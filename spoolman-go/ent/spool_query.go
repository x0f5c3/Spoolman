// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"spoolman-go/ent/filament"
	"spoolman-go/ent/predicate"
	"spoolman-go/ent/spool"
	"spoolman-go/ent/spoolfield"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SpoolQuery is the builder for querying Spool entities.
type SpoolQuery struct {
	config
	ctx          *QueryContext
	order        []spool.OrderOption
	inters       []Interceptor
	predicates   []predicate.Spool
	withFilament *FilamentQuery
	withExtra    *SpoolFieldQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SpoolQuery builder.
func (sq *SpoolQuery) Where(ps ...predicate.Spool) *SpoolQuery {
	sq.predicates = append(sq.predicates, ps...)
	return sq
}

// Limit the number of records to be returned by this query.
func (sq *SpoolQuery) Limit(limit int) *SpoolQuery {
	sq.ctx.Limit = &limit
	return sq
}

// Offset to start from.
func (sq *SpoolQuery) Offset(offset int) *SpoolQuery {
	sq.ctx.Offset = &offset
	return sq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sq *SpoolQuery) Unique(unique bool) *SpoolQuery {
	sq.ctx.Unique = &unique
	return sq
}

// Order specifies how the records should be ordered.
func (sq *SpoolQuery) Order(o ...spool.OrderOption) *SpoolQuery {
	sq.order = append(sq.order, o...)
	return sq
}

// QueryFilament chains the current query on the "filament" edge.
func (sq *SpoolQuery) QueryFilament() *FilamentQuery {
	query := (&FilamentClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(spool.Table, spool.FieldID, selector),
			sqlgraph.To(filament.Table, filament.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, spool.FilamentTable, spool.FilamentColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryExtra chains the current query on the "extra" edge.
func (sq *SpoolQuery) QueryExtra() *SpoolFieldQuery {
	query := (&SpoolFieldClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(spool.Table, spool.FieldID, selector),
			sqlgraph.To(spoolfield.Table, spoolfield.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, spool.ExtraTable, spool.ExtraColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Spool entity from the query.
// Returns a *NotFoundError when no Spool was found.
func (sq *SpoolQuery) First(ctx context.Context) (*Spool, error) {
	nodes, err := sq.Limit(1).All(setContextOp(ctx, sq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{spool.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sq *SpoolQuery) FirstX(ctx context.Context) *Spool {
	node, err := sq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Spool ID from the query.
// Returns a *NotFoundError when no Spool ID was found.
func (sq *SpoolQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sq.Limit(1).IDs(setContextOp(ctx, sq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{spool.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sq *SpoolQuery) FirstIDX(ctx context.Context) int {
	id, err := sq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Spool entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Spool entity is found.
// Returns a *NotFoundError when no Spool entities are found.
func (sq *SpoolQuery) Only(ctx context.Context) (*Spool, error) {
	nodes, err := sq.Limit(2).All(setContextOp(ctx, sq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{spool.Label}
	default:
		return nil, &NotSingularError{spool.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sq *SpoolQuery) OnlyX(ctx context.Context) *Spool {
	node, err := sq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Spool ID in the query.
// Returns a *NotSingularError when more than one Spool ID is found.
// Returns a *NotFoundError when no entities are found.
func (sq *SpoolQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sq.Limit(2).IDs(setContextOp(ctx, sq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{spool.Label}
	default:
		err = &NotSingularError{spool.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sq *SpoolQuery) OnlyIDX(ctx context.Context) int {
	id, err := sq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Spools.
func (sq *SpoolQuery) All(ctx context.Context) ([]*Spool, error) {
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryAll)
	if err := sq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Spool, *SpoolQuery]()
	return withInterceptors[[]*Spool](ctx, sq, qr, sq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sq *SpoolQuery) AllX(ctx context.Context) []*Spool {
	nodes, err := sq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Spool IDs.
func (sq *SpoolQuery) IDs(ctx context.Context) (ids []int, err error) {
	if sq.ctx.Unique == nil && sq.path != nil {
		sq.Unique(true)
	}
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryIDs)
	if err = sq.Select(spool.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sq *SpoolQuery) IDsX(ctx context.Context) []int {
	ids, err := sq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sq *SpoolQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryCount)
	if err := sq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sq, querierCount[*SpoolQuery](), sq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sq *SpoolQuery) CountX(ctx context.Context) int {
	count, err := sq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sq *SpoolQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryExist)
	switch _, err := sq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sq *SpoolQuery) ExistX(ctx context.Context) bool {
	exist, err := sq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SpoolQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sq *SpoolQuery) Clone() *SpoolQuery {
	if sq == nil {
		return nil
	}
	return &SpoolQuery{
		config:       sq.config,
		ctx:          sq.ctx.Clone(),
		order:        append([]spool.OrderOption{}, sq.order...),
		inters:       append([]Interceptor{}, sq.inters...),
		predicates:   append([]predicate.Spool{}, sq.predicates...),
		withFilament: sq.withFilament.Clone(),
		withExtra:    sq.withExtra.Clone(),
		// clone intermediate query.
		sql:  sq.sql.Clone(),
		path: sq.path,
	}
}

// WithFilament tells the query-builder to eager-load the nodes that are connected to
// the "filament" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *SpoolQuery) WithFilament(opts ...func(*FilamentQuery)) *SpoolQuery {
	query := (&FilamentClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withFilament = query
	return sq
}

// WithExtra tells the query-builder to eager-load the nodes that are connected to
// the "extra" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *SpoolQuery) WithExtra(opts ...func(*SpoolFieldQuery)) *SpoolQuery {
	query := (&SpoolFieldClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withExtra = query
	return sq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Registered time.Time `json:"registered,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Spool.Query().
//		GroupBy(spool.FieldRegistered).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sq *SpoolQuery) GroupBy(field string, fields ...string) *SpoolGroupBy {
	sq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SpoolGroupBy{build: sq}
	grbuild.flds = &sq.ctx.Fields
	grbuild.label = spool.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Registered time.Time `json:"registered,omitempty"`
//	}
//
//	client.Spool.Query().
//		Select(spool.FieldRegistered).
//		Scan(ctx, &v)
func (sq *SpoolQuery) Select(fields ...string) *SpoolSelect {
	sq.ctx.Fields = append(sq.ctx.Fields, fields...)
	sbuild := &SpoolSelect{SpoolQuery: sq}
	sbuild.label = spool.Label
	sbuild.flds, sbuild.scan = &sq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SpoolSelect configured with the given aggregations.
func (sq *SpoolQuery) Aggregate(fns ...AggregateFunc) *SpoolSelect {
	return sq.Select().Aggregate(fns...)
}

func (sq *SpoolQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sq); err != nil {
				return err
			}
		}
	}
	for _, f := range sq.ctx.Fields {
		if !spool.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sq.path != nil {
		prev, err := sq.path(ctx)
		if err != nil {
			return err
		}
		sq.sql = prev
	}
	return nil
}

func (sq *SpoolQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Spool, error) {
	var (
		nodes       = []*Spool{}
		_spec       = sq.querySpec()
		loadedTypes = [2]bool{
			sq.withFilament != nil,
			sq.withExtra != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Spool).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Spool{config: sq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := sq.withFilament; query != nil {
		if err := sq.loadFilament(ctx, query, nodes, nil,
			func(n *Spool, e *Filament) { n.Edges.Filament = e }); err != nil {
			return nil, err
		}
	}
	if query := sq.withExtra; query != nil {
		if err := sq.loadExtra(ctx, query, nodes,
			func(n *Spool) { n.Edges.Extra = []*SpoolField{} },
			func(n *Spool, e *SpoolField) { n.Edges.Extra = append(n.Edges.Extra, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (sq *SpoolQuery) loadFilament(ctx context.Context, query *FilamentQuery, nodes []*Spool, init func(*Spool), assign func(*Spool, *Filament)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Spool)
	for i := range nodes {
		fk := nodes[i].FilamentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(filament.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "filament_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (sq *SpoolQuery) loadExtra(ctx context.Context, query *SpoolFieldQuery, nodes []*Spool, init func(*Spool), assign func(*Spool, *SpoolField)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Spool)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(spoolfield.FieldSpoolID)
	}
	query.Where(predicate.SpoolField(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(spool.ExtraColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SpoolID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "spool_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (sq *SpoolQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sq.querySpec()
	_spec.Node.Columns = sq.ctx.Fields
	if len(sq.ctx.Fields) > 0 {
		_spec.Unique = sq.ctx.Unique != nil && *sq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sq.driver, _spec)
}

func (sq *SpoolQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(spool.Table, spool.Columns, sqlgraph.NewFieldSpec(spool.FieldID, field.TypeInt))
	_spec.From = sq.sql
	if unique := sq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sq.path != nil {
		_spec.Unique = true
	}
	if fields := sq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, spool.FieldID)
		for i := range fields {
			if fields[i] != spool.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if sq.withFilament != nil {
			_spec.Node.AddColumnOnce(spool.FieldFilamentID)
		}
	}
	if ps := sq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sq *SpoolQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sq.driver.Dialect())
	t1 := builder.Table(spool.Table)
	columns := sq.ctx.Fields
	if len(columns) == 0 {
		columns = spool.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sq.sql != nil {
		selector = sq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sq.ctx.Unique != nil && *sq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sq.predicates {
		p(selector)
	}
	for _, p := range sq.order {
		p(selector)
	}
	if offset := sq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SpoolGroupBy is the group-by builder for Spool entities.
type SpoolGroupBy struct {
	selector
	build *SpoolQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sgb *SpoolGroupBy) Aggregate(fns ...AggregateFunc) *SpoolGroupBy {
	sgb.fns = append(sgb.fns, fns...)
	return sgb
}

// Scan applies the selector query and scans the result into the given value.
func (sgb *SpoolGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sgb.build.ctx, ent.OpQueryGroupBy)
	if err := sgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SpoolQuery, *SpoolGroupBy](ctx, sgb.build, sgb, sgb.build.inters, v)
}

func (sgb *SpoolGroupBy) sqlScan(ctx context.Context, root *SpoolQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sgb.fns))
	for _, fn := range sgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sgb.flds)+len(sgb.fns))
		for _, f := range *sgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SpoolSelect is the builder for selecting fields of Spool entities.
type SpoolSelect struct {
	*SpoolQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ss *SpoolSelect) Aggregate(fns ...AggregateFunc) *SpoolSelect {
	ss.fns = append(ss.fns, fns...)
	return ss
}

// Scan applies the selector query and scans the result into the given value.
func (ss *SpoolSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ss.ctx, ent.OpQuerySelect)
	if err := ss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SpoolQuery, *SpoolSelect](ctx, ss.SpoolQuery, ss, ss.inters, v)
}

func (ss *SpoolSelect) sqlScan(ctx context.Context, root *SpoolQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ss.fns))
	for _, fn := range ss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
